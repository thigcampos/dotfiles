[user]
  email = thiago.campos@nan.systems

[core]
  sshCommand = ssh -i ~/.ssh/nan-systems

[commit]
  gpgSign = false

[diff "docx"]
    textconv = pandoc --from=docx --to=markdown
    prompt = false
